{
  "scripts": {
    "build:js": "esbuild src/frontend/js/main.ts --bundle --outfile=src/assets/static/bundle.js",
    "build:css": "esbuild src/frontend/css/app.css --bundle --outfile=src/assets/static/bundle.css",
    "build": "npm run build:js && npm run build:css"
  },
  "devDependencies": {
    "esbuild": "0.28.1"
  },
  "dependencies": {
    "@fluent/bundle": "^0.19.1",
    "bootstrap": "4.5",
    "vue": "^2.7.16"
  }
}
